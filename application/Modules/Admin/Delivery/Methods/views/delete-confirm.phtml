<?php
namespace JetApplicationModule\Admin\Delivery\Methods;

use JetApplication\Delivery_Method;

use Jet\Http_Request;
use Jet\UI;
use Jet\MVC_View;
use Jet\Tr;

/**
 * @var MVC_View   $this
 * @var Delivery_Method       $delivery_method
 */
$delivery_method = $this->getRaw( 'delivery_method' );
$router = $this->getController()->getControllerRouter();

?>

<div class="row">
	<div class="col-md-12" id="main-col">

		<?=UI::button_goBack()?>
		<p>
			<?=Tr::_( 'Do you really want to delete this delivery method?' );?>
			<br/>
			<a href="<?=$router->action('edit')->URI( $delivery_method->getCode() );?>"
			   style="font-weight: bolder;font-size: 20px;"><?=$delivery_method->getInternalName();?> (<?=$delivery_method->getInternalName();?>)</a>
		</p>

		<form action="<?=Http_Request::currentURI()?>" method="post">
			<input type="hidden" name="delete" value="yes">
			<?=UI::button_delete()->setType( 'submit' )?>
		</form>
	</div>
</div>
<?php
/**
 * @copyright Copyright (c) Miroslav Marek <mirek.marek@web-jet.cz>
 * @license EUPL 1.2  https://eupl.eu/1.2/en/
 * @author Miroslav Marek <mirek.marek@web-jet.cz>
 */

namespace JetApplicationModule\Admin\Entity\Edit;

use Jet\MVC_View;
use JetApplication\Admin_Managers;
use JetApplication\EShopEntity_Basic;

/**
 * @var MVC_View $this
 * @var EShopEntity_Basic $item
 */

$item = $this->getRaw('item');


if(!$item->getId() || !$item->isEditable()) {
	return;
}
?>
<div style="position: fixed;top: 60px;right: 2px;z-index: 998;">
	<?=Admin_Managers::Image()?->commonImageManager( $item::getEntityType(), $item->getId() );?>
	<?=Admin_Managers::Document()?->commonDocumentManager( $item::getEntityType(), $item->getId() );?>
	<?=Admin_Managers::MagicTags()?->renderTool();?>
</div>

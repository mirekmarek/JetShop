<?php
use Jet\Form;
use Jet\MVC_View;

/**
 * @var MVC_View $this
 * @var Form $form
 */
$form = $this->getRaw('form');
$item = $this->getRaw('item');

$schema = [
	'eshop',
	'internal_name',
	'internal_code',
	'internal_notes',
	'active_from',
	'active_till',
];


foreach($schema as $f) {
	if($form->fieldExists($f)) {
		echo $form->field($f);
	}
}
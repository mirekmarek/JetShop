<?php
/**
 *
 * @copyright
 * @license
 * @author
 */
namespace JetApplicationModule\Admin\Invoices;

use Jet\MVC_View;
use JetApplication\Admin_Managers;
use JetApplication\Invoice;

/**
 * @var MVC_View $this
 * @var Invoice $item
 */

$item = $this->getRaw('item');

echo Admin_Managers::PriceFormatter()->formatWithCurrencyByHasVAT( $item->hasVAT(), $item->getCurrency(), $item->getTotal() );

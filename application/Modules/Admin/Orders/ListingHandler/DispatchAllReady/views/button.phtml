<?php
namespace JetApplicationModule\Admin\Orders;

use Jet\MVC_View;
use Jet\Tr;
use Jet\UI;
use Jet\UI_button;


/**
 * @var MVC_View $this
 */

if(!Main::getCurrentUserCanEdit()) {
	return;
}

echo UI::button( Tr::_('Dispatch all orders that are ready') )
	->setClass(UI_button::CLASS_SUCCESS)
	->setOnClick("$('#dispatch_all_ready').modal('show');");
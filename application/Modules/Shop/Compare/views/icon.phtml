<?php
use Jet\MVC_View;
use Jet\UI;
use Jet\UI_button;
use JetApplication\Shop_Pages;

/**
 * @var MVC_View $this
 */

echo UI::button( '&nbsp;'.count($this->getRaw('product_ids')) )
	->setIcon('scale')
	->setClass( UI_button::CLASS_LIGHT )
	->setUrl( Shop_Pages::Compare()->getURL() );


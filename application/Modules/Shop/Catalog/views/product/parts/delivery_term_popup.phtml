<?php
namespace JetShop;

use Jet\Mvc_View;

/**
 * @var Mvc_View $this
 * @var Product $product
 */

$product = $this->getRaw('product');
?>

<div class="availability">
	<?php
	/*
	if (($prod_quantity = $variant->getProductsQuantity())>0) {
		if ($prod_quantity >= 10) {
			echo '<span class="">'.SKLADEM_10AVIC . sklonovaniKusu(10) . '</span>';
		}
		else {
			echo '<span class="">'.SKLADEM_ . $prod_quantity . ' ' . sklonovaniKusu($prod_quantity) . '</span>';
		}
		?>
		<span class="available" id="available-<?= $variant->getId() ?>" onclick="ga('send', 'event', 'detail delivery detail', 'click', '<?= $variant->getProductsName() ?>');"><?= KDY_ZBOZI_DOSTANU_ ?></span>
		<?php
		echo view::renderPartial('parts/delivery_popup', ['product'=>$variant,'box_params'=>' id="delivery-' . $variant->getId() . '"']);
	}
	else {
		echo '<span class="">' . $variant->getDeliveryText('') . '</span>';
	}
	*/
	?>
</div>


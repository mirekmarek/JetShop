<?php
use Jet\MVC_View;
use Jet\UI;
use Jet\UI_button;
use JetApplication\EShop_Pages;

/**
 * @var MVC_View $this
 */


echo UI::button( '&nbsp;'.count($this->getRaw('product_ids')) )
	->setIcon('heart')
	->setClass( UI_button::CLASS_LIGHT )
	->setUrl( EShop_Pages::Wishlist()->getURL() );

